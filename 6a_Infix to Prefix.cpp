#include <iostream>
#include <math.h>

using namespace std;

int main(){
      double Celsius, Fahrenheit;

      cout << "Enter the temperate in Celsius:" << endl;
      cin >> Celsius;
      
      // Formula to convert Celsius to Fahrenheit using "Infix to Prefix" method
      Fahrenheit = (+(*Celsius(/95))32);

      cout << "Temperature in Fahrenheit: " << Fahrenheit << endl;
      
      return 0;
}
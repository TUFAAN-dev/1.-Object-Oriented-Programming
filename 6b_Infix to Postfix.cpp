#include <iostream>
#include <math.h>

using namespace std;

int main(){
      double Celsius, Fahrenheit;

      cout << "Enter the temperate in Celsius:" << endl;
      cin >> Celsius;
      
      // Formula to convert Celsius to Fahrenheit using "Infix to Postfix" method
      Fahrenheit = ( ( Celsius ( 9 5 / ) * ) 32 + ) ;

      cout << "Temperature in Fahrenheit: " << Fahrenheit << endl;
      
      return 0;
}